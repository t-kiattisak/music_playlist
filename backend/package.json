{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "seed": "bun run prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "hono": "^4.7.7",
    "prisma": "^6.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
